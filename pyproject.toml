[project]
name = "lego-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "requests>=2.32.5"
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "requests-mock>=1.12.1"
]